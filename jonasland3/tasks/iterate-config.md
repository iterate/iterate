---
dependsOn: ["customer-git-repo.md"]
---

Define the iterate config format. Every iterate project's customer repo will have a config file (likely TypeScript) that defines project settings, overrides, and extensions. Need to find the right shape for it — what it controls, how it's loaded, and how it interacts with the platform.
