---
dependsOn: ["customer-git-repo.md"]
---

Allow customers to override the default create-agent behaviour from their git repo. The vanilla implementation (webchat, terminal UI, etc.) should be replaceable with a customer-defined agent creation flow.
