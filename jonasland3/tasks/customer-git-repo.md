Introduce the concept of a customer git repository. Every iterate project will generally have a customer-provided config/code repo that coding agents iterate on. This is the foundation for userspace customisation — the repo is where customers define overrides, config, and extensions.

Related: iterate-config.md, userspace-override-create-agent.md, userspace-override-key-functionality.md
