# this file is generated by release.ts, do not edit it manually

name: Release
permissions:
  contents: write
on:
  schedule:
    - cron: 0 20 * * *
  workflow_dispatch:
    inputs:
      release_title:
        description: Title for the release (e.g. 'Daily scheduled release')
        required: false
        default: ""
      trigger_description:
        description: Description of what triggered this release
        required: false
        default: ""
  workflow_call:
    inputs:
      release_title:
        description: Title for the release (e.g. 'Daily scheduled release')
        required: false
        type: string
        default: ""
      trigger_description:
        description: Description of what triggered this release
        required: false
        type: string
        default: ""
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Set trigger context
        id: context
        run: |-
          # Determine trigger type and set appropriate defaults
          if [ "${{ github.event_name }}" = "schedule" ]; then
            RELEASE_TITLE="Daily scheduled release"
            TRIGGER_DESC="scheduled release"
          elif [ "${{ github.event_name }}" = "workflow_call" ]; then
            RELEASE_TITLE="${{ inputs.release_title }}"
            TRIGGER_DESC="${{ inputs.trigger_description }}"
            # Use defaults if not provided
            if [ -z "$RELEASE_TITLE" ]; then
              RELEASE_TITLE="Release"
            fi
            if [ -z "$TRIGGER_DESC" ]; then
              TRIGGER_DESC="workflow_call"
            fi
          else
            # workflow_dispatch
            RELEASE_TITLE="${{ inputs.release_title }}"
            TRIGGER_DESC="${{ inputs.trigger_description }}"
            if [ -z "$RELEASE_TITLE" ]; then
              RELEASE_TITLE="Manual release"
            fi
            if [ -z "$TRIGGER_DESC" ]; then
              TRIGGER_DESC="manual dispatch"
            fi
          fi

          echo "release_title=$RELEASE_TITLE" >> $GITHUB_OUTPUT
          echo "trigger_desc=$TRIGGER_DESC" >> $GITHUB_OUTPUT
      - name: Get release info
        id: release_info
        run: |-
          LAST_RELEASE=$(git describe --tags --abbrev=0 || echo '')
          echo "last_release=$LAST_RELEASE" >> $GITHUB_OUTPUT
          echo "release_name=v$(TZ=Europe/London date +%Y-%m-%d-%H-%M-%S)" >> $GITHUB_OUTPUT
      - name: Check for changes
        id: check_changes
        run: |-
          LAST_RELEASE="${{ steps.release_info.outputs.last_release }}"
          if [ "$LAST_RELEASE" = "" ]; then
            LAST_RELEASE=$(git rev-parse HEAD~1)
          fi

          COMMITS=$(git log $LAST_RELEASE..HEAD --oneline | wc -l)
          echo "commits=$COMMITS" >> $GITHUB_OUTPUT

          if [ "$COMMITS" -eq "0" ]; then
            echo "No new commits since last release"
          else
            echo "Found $COMMITS new commits since $LAST_RELEASE"
          fi
      - name: Write changelog
        if: steps.check_changes.outputs.commits != '0'
        run: |-
          add_to_changelog() {
            echo "$1" >> changelog.md
            echo "" >> changelog.md
          }

          LAST_RELEASE="${{ steps.release_info.outputs.last_release }}"
          RELEASE_NAME="${{ steps.release_info.outputs.release_name }}"
          TRIGGER_DESC="${{ steps.context.outputs.trigger_desc }}"

          if [ "$LAST_RELEASE" = "" ]; then
            LAST_RELEASE=$(git rev-parse HEAD~1)
            add_to_changelog "No previous release found - using HEAD~1 ($LAST_RELEASE)"
          else
            add_to_changelog "Last tagged release: [$LAST_RELEASE](${{ github.event.repository.html_url }}/releases/$LAST_RELEASE) ([compare link](${{ github.event.repository.html_url }}/compare/$LAST_RELEASE...$RELEASE_NAME))"
          fi

          write_git_changes() {
            glob=$1
            description=${2:-$glob}

            changes=$(git log $LAST_RELEASE..HEAD --oneline -- $glob | sed 's/^/- /g')

            if [ "$changes" != "" ]; then
              add_to_changelog "## $description"
              add_to_changelog "$changes"
            fi
          }

          write_git_changes '.' 'changes'

          add_to_changelog "Triggered by: $TRIGGER_DESC"

          add_to_changelog "[Comparison with current main](${{ github.event.repository.html_url }}/compare/$RELEASE_NAME...main)"

          echo "echoing changes for debugging:"
          cat changelog.md
      - if: steps.check_changes.outputs.commits != '0'
        name: create_release
        id: create_release
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.ITERATE_BOT_GITHUB_TOKEN }}
          script: |-
            github.log = {
              ...console,
              debug: () => {},
            };

            // write a shimmed module of "fs" that actions/github-script can require (see https://github.com/actions/github-script?tab=readme-ov-file#this-action)
            require("fs").writeFileSync(
              ".github/ts-workflows/workflows/release.ts.fs-bc7b36fe4d2924e49800d9b3dc4a325c-tsx-shim.cjs",
              "module.exports.load = () => import(\"fs\")",
            );

            const vars = {
              github,
              context,
              core,
              glob,
              io,
              require,
            };

            const __handler = async function create_release(
              {
                github,
                context,
              },
            ) {
              const {
                promises: fs,
              } = await require(
                ".github/ts-workflows/workflows/release.ts.fs-bc7b36fe4d2924e49800d9b3dc4a325c-tsx-shim.cjs" /* <-- shimmed module of "fs" that actions/github-script can require */,
              ).load();

              await github.rest.repos.createRelease({
                ...context.repo,
                tag_name: "${{ steps.release_info.outputs.release_name }}",
                name: "${{ steps.release_info.outputs.release_name }}",

                body: ["${{ steps.context.outputs.release_title }}", //
                "", await fs.readFile("changelog.md", "utf8")].join("\n"),
              });
            };

            return __handler(vars);