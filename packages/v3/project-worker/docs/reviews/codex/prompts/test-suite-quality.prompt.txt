You are reviewing /Users/jonastemplestein/.herdr/worktrees/iterate/simplification/packages/v3/project-worker (a git worktree of the iterate monorepo at /Users/jonastemplestein/.herdr/worktrees/iterate/simplification, branch wip/kernel-wayfinder-2026-07-30). You run in a READ-ONLY sandbox: read anything, run read-only commands (grep, cat, git log/diff/show, wc, node scripts that only read), write nothing. Your FINAL MESSAGE is captured verbatim into a markdown report file, so write it AS the report: start with a title line, then a 5-line summary, then ranked findings, each with `file:line` (verify the line exists), the mechanism, why it matters, and a concrete recommendation with size (lines/files). End with a "checked and fine" list and a confidence note. No preamble, no chit-chat, no questions back.

CONTEXT. This package is a clean-room rewrite of Iterate's "iterate context" (itx): one stateless worker (/api, capnweb — Kenton Varda's RPC library, fork @iterate-com/capnweb) proxying over Workers RPC to one SQLite Durable Object per {projectId, path} (IterateContextDurableObject). The DO holds an append-only event log (src/stream/stream.ts), reduces ONE inline core processor at commit (identity, incarnation, paused, the rewrite-rule map, the subscription map — src/stream/core-processor.ts), runs ONE delivery loop after every commit (src/stream/subscription-delivery.ts: push to facets and lent rpc stubs, a stream-kept at-least-once cursor for anything else), hosts loaded DurableObject classes as FACETS (itx.facets.get(name, { source, cacheKey?, className }) → ctx.facets.get) and stateless WorkerEntrypoints (itx.workers.get({ source, cacheKey?, className?, props? })) through the Worker Loader (src/context/worker-loader.ts), lends clients' live rpc stubs via hibernatable "pager" WebSockets (src/context/rpc-stub-directory.ts + rpc-stub-relay.ts) so the DO can hibernate with clients attached, and quiesces idle facets and borrowed stubs on a 60 s alarm. Every dotted call itx.a.b(x) is ONE itx expression (src/context/expression.ts) rewritten through the rule table (src/context/itx-expression-rewriting.ts) to a built-in root (src/context/built-ins.ts) and executed (src/context/dispatch.ts). The edge (src/iterate-context.ts) is a PROXY: cd, invoke, provide(match, stub | expression | null), subscribe, enableProcessor, disableProcessor; every verb builds an event and appends it. Read FIRST: docs/itx-surface-as-built.md (the map), LAYERS.md, the last ~14 entries of BUILD-LOG.md (today's commits), then the source under src/ (not tests) and the tests under e2e/, __workers-tests__/, src/**/*.test.ts. Other reviewers' reports already exist in docs/reviews/2026-09-02-*.md — read them, do not duplicate, build on them, and say where you DISAGREE.

OWNER DOCTRINE (Jonas): everything less abstract and less magical; FULLY QUALIFIED identifier names (never #borrowed, always #borrowedRpcStubs); no invented concept nouns; events are the interface; one door per concept; trusted clients (a malicious client is NOT a bug — do not report hardening); reduce complexity and LOC; align with Kenton Varda's SHIPPED Cloudflare direction (facets, ctx.props, ctx.exports, Worker Loader get(id, getCode)) and justify every divergence; every stream-processor side effect must tolerate at-least-once delivery and be idempotent, so a green-path speedup that occasionally DUPLICATES a delivery is acceptable for a meaningful gain, a LOST delivery never is; the DO must hibernate with any number of clients attached.

TOPIC: TEST SUITE QUALITY. Read every test under e2e/, __workers-tests__/, src/**/*.test.ts and e2e/support/*. Find: behaviours in src/ with no test (map each public door and each documented invariant in docs/itx-surface-as-built.md to the test that proves it; list the holes), tests that assert implementation details instead of behaviour, overlapping tests that pin the same thing, timing-sensitive or sleep-based tests likely to flake (list each sleep/until with its budget), tests whose titles no longer describe them, fixtures that hide the real API shape (e2e/support/client.ts returns any everywhere — should the e2e lane be typed?), the 'test.fails' convention for known-red proofs (list them and whether each still describes a real bug), the three lanes' split (unit/node, vitest-pool-workers, e2e with a live worker) and whether tests sit in the cheapest lane that can prove them, and what a 'hibernation actually happens' proof would need. Recommend deletions and merges first, then the missing proofs ranked by risk.
