{
  // The SDK unit tests, with tsconfig.next.json's Worker settings (vitest runs them untyped).
  "extends": "./tsconfig.next.json",
  "files": [],
  "include": ["src/**/*.test.ts", "src/**/*.test.tsx", "src/test-support"],
  "exclude": ["src/cli.test.ts"],
  "compilerOptions": {
    "noEmit": true,
    "declaration": false,
    "emitDeclarationOnly": false,
    "types": ["@cloudflare/workers-types/experimental", "node"]
  }
}
