# iterate-internal TODOs

- [ ] Consider removing build.ts in favor of a simpler esbuild CLI command in package.json
- [ ] Move this to a separate repo at ~/src/github.com/iterate-com/iterate-internal
