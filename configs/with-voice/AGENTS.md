# Voice project

This project starts with a small browser-based voice surface. Keep the root
worker directly runnable, and preserve a text fallback for browsers where
speech synthesis is unavailable.
