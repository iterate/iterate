diff --git a/lib/cloudflare/api.js b/lib/cloudflare/api.js
index a5efe2b..3e246e3 100644
--- a/lib/cloudflare/api.js
+++ b/lib/cloudflare/api.js
@@ -119,6 +119,10 @@ export class CloudflareApi {
         else if (init.headers) {
             headers = init.headers;
         }
+        // Node 26 + undici Agent can expose gzip response bytes without the
+        // content-encoding header, which makes response.json() parse gzip data.
+        if (!Object.keys(headers).some((key) => key.toLowerCase() === "accept-encoding"))
+            headers["accept-encoding"] = "identity";
         headers = {
             ...(await CloudflareAuth.formatHeadersWithRefresh({
                 profile: this.profile,
diff --git a/lib/cloudflare/bindings.d.ts b/lib/cloudflare/bindings.d.ts
index 202181c..131a784 100644
--- a/lib/cloudflare/bindings.d.ts
+++ b/lib/cloudflare/bindings.d.ts
@@ -42,10 +42,21 @@ export declare namespace Bindings {
 /**
  * L2 Binding Resources.
  */
-export type Binding = Ai | Assets | Container | CloudflareSecret | CloudflareSecretRef | D1Database | DispatchNamespace | AnalyticsEngineDataset | DurableObjectNamespace<any> | Hyperdrive | HyperdriveRef | Images | KVNamespace | Pipeline | Queue | RateLimit | R2Bucket | {
+export type Binding = Ai | Artifacts | Assets | Container | CloudflareSecret | CloudflareSecretRef | D1Database | DispatchNamespace | AnalyticsEngineDataset | DurableObjectNamespace<any> | Hyperdrive | HyperdriveRef | Images | KVNamespace | Pipeline | Queue | RateLimit | R2Bucket | SendEmail | {
     type: "kv_namespace";
     id: string;
 } | Secret | SecretKey | string | VectorizeIndex | Worker | Worker.DevDomain | Worker.DevUrl | WorkerStub | WorkerRef | WorkerEntrypoint | WorkerLoader | Workflow | BrowserRendering | VersionMetadata | Self | Json;
+export type Artifacts = {
+    type: "artifacts";
+    namespace: string;
+};
+export type SendEmail = {
+    type: "send_email";
+    destinationAddress?: string;
+    allowedDestinationAddresses?: string[];
+    allowedSenderAddresses?: string[];
+    remote?: boolean;
+};
 export type Self<RPC extends Rpc.WorkerEntrypointBranded = Rpc.WorkerEntrypointBranded> = {
     type: "cloudflare::Worker::Self";
     __entrypoint__?: string;
@@ -65,7 +76,22 @@ export declare function Json<const T>(value: T): Json<T>;
 /**
  * Union type for all Worker binding types (API spec)
  */
-export type WorkerBindingSpec = WorkerBindingAI | WorkerBindingAnalyticsEngine | WorkerBindingAssets | WorkerBindingBrowserRendering | WorkerBindingD1 | WorkerBindingDispatchNamespace | WorkerBindingDurableObjectNamespace | WorkerBindingHyperdrive | WorkerBindingImages | WorkerBindingJson | WorkerBindingKVNamespace | WorkerBindingMTLSCertificate | WorkerBindingPipeline | WorkerBindingPlainText | WorkerBindingQueue | WorkerBindingRateLimit | WorkerBindingR2Bucket | WorkerBindingSecretKey | WorkerBindingSecretText | WorkerBindingSecretsStore | WorkerBindingSecretsStoreSecret | WorkerBindingService | WorkerBindingStaticContent | WorkerBindingTailConsumer | WorkerBindingVectorize | WorkerBindingVersionMetadata | WorkerBindingWasmModule | WorkerBindingWorkerLoader | WorkerBindingWorkflow;
+export type WorkerBindingSpec = WorkerBindingAI | WorkerBindingAnalyticsEngine | WorkerBindingArtifacts | WorkerBindingAssets | WorkerBindingBrowserRendering | WorkerBindingD1 | WorkerBindingDispatchNamespace | WorkerBindingDurableObjectNamespace | WorkerBindingHyperdrive | WorkerBindingImages | WorkerBindingJson | WorkerBindingKVNamespace | WorkerBindingMTLSCertificate | WorkerBindingPipeline | WorkerBindingPlainText | WorkerBindingQueue | WorkerBindingRateLimit | WorkerBindingR2Bucket | WorkerBindingSecretKey | WorkerBindingSecretText | WorkerBindingSecretsStore | WorkerBindingSecretsStoreSecret | WorkerBindingSendEmail | WorkerBindingService | WorkerBindingStaticContent | WorkerBindingTailConsumer | WorkerBindingVectorize | WorkerBindingVersionMetadata | WorkerBindingWasmModule | WorkerBindingWorkerLoader | WorkerBindingWorkflow;
+export interface WorkerBindingArtifacts {
+    /** The name of the binding */
+    name: string;
+    /** Type identifier for Artifacts binding */
+    type: "artifacts";
+    /** Cloudflare Artifacts namespace */
+    namespace: string;
+}
+export interface WorkerBindingSendEmail {
+    name: string;
+    type: "send_email";
+    destination_address?: string;
+    allowed_destination_addresses?: string[];
+    allowed_sender_addresses?: string[];
+}
 /**
  * AI binding type
  */
@@ -414,4 +440,4 @@ export interface WorkerBindingWorkerLoader {
     /** Type identifier for Worker Loader binding */
     type: "worker_loader";
 }
-//# sourceMappingURL=bindings.d.ts.map
\ No newline at end of file
+//# sourceMappingURL=bindings.d.ts.map
diff --git a/lib/cloudflare/miniflare/build-worker-options.js b/lib/cloudflare/miniflare/build-worker-options.js
index be105c3..a555191 100644
--- a/lib/cloudflare/miniflare/build-worker-options.js
+++ b/lib/cloudflare/miniflare/build-worker-options.js
@@ -124,11 +124,16 @@ export const buildWorkerOptions = async (input) => {
                 };
                 // This matches Wrangler configuration for exposing durable objects as an entrypoint.
                 // See https://github.com/cloudflare/workers-sdk/blob/ae0c806087c203da6a3d7da450e8fabe0d81c987/packages/wrangler/src/dev/miniflare/index.ts#L1266
-                options.unsafeDirectSockets.push({
-                    entrypoint: binding.className,
-                    serviceName: binding.scriptName,
-                    proxy: true,
-                });
+                // Wrangler only exposes a worker's OWN durable object classes this way
+                // (cross-script bindings resolve through the dev registry proxy). Pushing a
+                // direct socket for a cross-script binding points workerd at a service that
+                // may not exist in this Miniflare instance and fails config assembly.
+                if (binding.scriptName == null || binding.scriptName === input.name) {
+                    options.unsafeDirectSockets.push({
+                        entrypoint: binding.className,
+                        proxy: true,
+                    });
+                }
                 break;
             }
             case "hyperdrive": {
diff --git a/lib/cloudflare/worker-metadata.js b/lib/cloudflare/worker-metadata.js
index d521d20..74b0ff0 100644
--- a/lib/cloudflare/worker-metadata.js
+++ b/lib/cloudflare/worker-metadata.js
@@ -91,6 +91,21 @@ export async function prepareWorkerMetadata(api, props) {
         return [];
     });
     const observability = camelToSnakeObjectDeep(props.observability);
+    const durableObjectBindingTags = Object.entries(props.bindings ?? {}).flatMap(([bindingName, binding]) => isDurableObjectNamespace(binding) || isContainer(binding)
+        ? // TODO(sam): base64 encode if contains `:`?
+            [`alchemy:do:${binding.id}:${bindingName}`]
+        : []);
+    const reservedWorkerTags = [
+        // encode the migraiton tag if there is one so we can avoid the failed PutWorker after adoption
+        ...(newMigrationTag ? [`alchemy:migration-tag:${newMigrationTag}`] : []),
+        ...(props.tags ?? []),
+    ].slice(0, 10);
+    // Cloudflare caps Workers at 10 tags. Keep migration/user tags first, then
+    // fit as many Durable Object stable-id mapping tags as possible.
+    const workerTags = [
+        ...reservedWorkerTags,
+        ...durableObjectBindingTags.slice(0, 10 - reservedWorkerTags.length),
+    ];
     // Prepare metadata with bindings
     const meta = {
         compatibility_date: props.compatibilityDate,
@@ -104,15 +119,7 @@ export async function prepareWorkerMetadata(api, props) {
         logpush: props.logpush ?? false,
         // TODO(sam): base64 encode instead? 0 collision risk vs readability.
         tags: [
-            // encode a mapping table of Durable Object stable ID -> binding name
-            // we use this to reliably compute class migrations based on server-side state
-            ...Object.entries(props.bindings ?? {}).flatMap(([bindingName, binding]) => isDurableObjectNamespace(binding) || isContainer(binding)
-                ? // TODO(sam): base64 encode if contains `:`?
-                    [`alchemy:do:${binding.id}:${bindingName}`]
-                : []),
-            // encode the migraiton tag if there is one so we can avoid the failed PutWorker after adoption
-            ...(newMigrationTag ? [`alchemy:migration-tag:${newMigrationTag}`] : []),
-            ...(props.tags ?? []),
+            ...workerTags,
         ],
         migrations: {
             old_tag: oldMigrationTag,
@@ -330,6 +337,22 @@ export async function prepareWorkerMetadata(api, props) {
                 name: bindingName,
             });
         }
+        else if (binding.type === "artifacts") {
+            meta.bindings.push({
+                type: "artifacts",
+                name: bindingName,
+                namespace: binding.namespace,
+            });
+        }
+        else if (binding.type === "send_email") {
+            meta.bindings.push({
+                type: "send_email",
+                name: bindingName,
+                destination_address: binding.destinationAddress,
+                allowed_destination_addresses: binding.allowedDestinationAddresses,
+                allowed_sender_addresses: binding.allowedSenderAddresses,
+            });
+        }
         else if (binding.type === "version_metadata") {
             meta.bindings.push({
                 type: "version_metadata",
@@ -469,4 +492,4 @@ export async function getWorkerSettings(api, workerName) {
         throw error;
     });
 }
-//# sourceMappingURL=worker-metadata.js.map
\ No newline at end of file
+//# sourceMappingURL=worker-metadata.js.map
diff --git a/lib/cloudflare/worker.js b/lib/cloudflare/worker.js
index 7912452..da9bc9f 100644
--- a/lib/cloudflare/worker.js
+++ b/lib/cloudflare/worker.js
@@ -263,15 +263,7 @@ const _Worker = Resource("cloudflare::Worker", {
         const oldName = this.output.name ?? this.output.id;
         const newName = options.name;
         if (oldName && oldName !== newName) {
-            if (options.dispatchNamespace) {
-                this.replace(true);
-            }
-            else {
-                const renameResponse = await api.patch(`/accounts/${api.accountId}/workers/services/${oldName}`, { id: newName });
-                if (!renameResponse.ok) {
-                    await handleApiError(renameResponse, "rename", "worker", oldName);
-                }
-            }
+            this.replace(true);
         }
     }
     const assets = options.assets
diff --git a/lib/cloudflare/wrangler.json.js b/lib/cloudflare/wrangler.json.js
index 43dfd76..c059276 100644
--- a/lib/cloudflare/wrangler.json.js
+++ b/lib/cloudflare/wrangler.json.js
@@ -138,6 +138,8 @@ async function processBindings(spec, bindings, eventSources, workerName, workerC
     const ratelimits = [];
     const containers = [];
     const workerLoaders = [];
+    const artifacts = [];
+    const sendEmail = [];
     for (const eventSource of eventSources ?? []) {
         if (isQueueEventSource(eventSource)) {
             queues.consumers.push({
@@ -321,6 +323,22 @@ async function processBindings(spec, bindings, eventSources, workerName, workerC
                 remote: true,
             };
         }
+        else if (binding.type === "artifacts") {
+            artifacts.push({
+                binding: bindingName,
+                namespace: binding.namespace,
+                remote: true,
+            });
+        }
+        else if (binding.type === "send_email") {
+            sendEmail.push({
+                name: bindingName,
+                destination_address: binding.destinationAddress,
+                allowed_destination_addresses: binding.allowedDestinationAddresses,
+                allowed_sender_addresses: binding.allowedSenderAddresses,
+                remote: binding.remote ?? true,
+            });
+        }
         else if (binding.type === "analytics_engine") {
             analyticsEngineDatasets.push({
                 binding: bindingName,
@@ -466,5 +484,11 @@ async function processBindings(spec, bindings, eventSources, workerName, workerC
     if (workerLoaders.length > 0) {
         spec.worker_loaders = workerLoaders;
     }
+    if (artifacts.length > 0) {
+        spec.artifacts = artifacts;
+    }
+    if (sendEmail.length > 0) {
+        spec.send_email = sendEmail;
+    }
 }
-//# sourceMappingURL=wrangler.json.js.map
\ No newline at end of file
+//# sourceMappingURL=wrangler.json.js.map
