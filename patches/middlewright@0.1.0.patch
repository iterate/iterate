diff --git a/dist/plugin-system.d.ts b/dist/plugin-system.d.ts
index a6a28b3733db940ac99a3fe126c7b0d20698fd59..18983065fa9fc85f4381373fe51a990910b13c80 100644
--- a/dist/plugin-system.d.ts
+++ b/dist/plugin-system.d.ts
@@ -2,7 +2,7 @@ import Emittery from "emittery";
 import type { Locator, Page, TestInfo } from "@playwright/test";
 declare const oneArgMethods: readonly ["fill", "type", "press"];
 type OneArgMethod = (typeof oneArgMethods)[number];
-declare const overrideableMethods: ("fill" | "type" | "press" | "blur" | "clear" | "click" | "dblclick" | "focus" | "hover" | "waitFor")[];
+declare const overrideableMethods: ("fill" | "type" | "press" | "blur" | "clear" | "click" | "dblclick" | "focus" | "hover" | "inputValue" | "waitFor")[];
 type OverrideableMethod = (typeof overrideableMethods)[number];
 export type LocatorWithOriginal = Locator & {
     [K in OverrideableMethod as `${K}_original`]: Locator[K];
diff --git a/dist/plugin-system.js b/dist/plugin-system.js
index 74841e6628fb775d06454b3a6d610ebe006f0247..fa3738456ab841aa8f7a467907663cf79f1fe40b 100644
--- a/dist/plugin-system.js
+++ b/dist/plugin-system.js
@@ -22,6 +22,7 @@ const overrideableMethods = [
     "blur",
     "focus",
     "hover",
+    "inputValue",
     ...oneArgMethods,
 ];
 /**
diff --git a/dist/plugins/spinner-waiter.js b/dist/plugins/spinner-waiter.js
index d58b62dab7b1844d64fd4e295bfc421c4a5b662a..5d10e7c27df82ff25d3382c2db71692242a60ee0 100644
--- a/dist/plugins/spinner-waiter.js
+++ b/dist/plugins/spinner-waiter.js
@@ -101,6 +101,11 @@ export const spinnerWaiter = Object.assign((options = {}) => {
             const spinnerLocator = page.locator(spinnerSelector);
             const loading = await loadingVisible(page, spinnerLocator);
             if (!loading) {
+                // The target may have appeared while we checked loading. Do not
+                // turn a now-ready action into a 1ms timeout just because its
+                // spinner disappeared between the two observations.
+                if (await locatorIsReady(locator, method))
+                    return next();
                 // No spinner, no navigation - call action, suggest a spinner if it fails
                 settings.log(`${locator} not ready, nothing loading, failing fast`);
                 try {
diff --git a/src/plugin-system.ts b/src/plugin-system.ts
index 7788774d2217015778a226317f5b760b62038db7..228dd1bbb4362f0996ca77aa2126ff6710434eaf 100644
--- a/src/plugin-system.ts
+++ b/src/plugin-system.ts
@@ -27,6 +27,7 @@ const overrideableMethods = [
   "blur",
   "focus",
   "hover",
+  "inputValue",
   ...oneArgMethods,
 ] satisfies (keyof Locator)[];
 type OverrideableMethod = (typeof overrideableMethods)[number];
diff --git a/src/plugins/spinner-waiter.ts b/src/plugins/spinner-waiter.ts
index 43a5558f98ed82fa7c49a2bafff1fb968408fdac..11836542006c4188cba6c3286cdd93ad89c4245b 100644
--- a/src/plugins/spinner-waiter.ts
+++ b/src/plugins/spinner-waiter.ts
@@ -134,6 +134,10 @@ export const spinnerWaiter = Object.assign(
         const loading = await loadingVisible(page, spinnerLocator);
 
         if (!loading) {
+          // The target may have appeared while we checked loading. Do not
+          // turn a now-ready action into a 1ms timeout just because its
+          // spinner disappeared between the two observations.
+          if (await locatorIsReady(locator, method)) return next();
           // No spinner, no navigation - call action, suggest a spinner if it fails
           settings.log(`${locator} not ready, nothing loading, failing fast`);
           try {
