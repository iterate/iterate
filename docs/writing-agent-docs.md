# Instruction maintenance

`AGENTS.md` is canonical. Compatibility entrypoints such as `CLAUDE.md` are symlinks, never duplicate instruction files.

Keep local facts and entrypoints short. Put longer explanations beside the owning code and link to them. Correct or remove stale guidance before adding more. Verify links when moving or deleting documentation.
