# Task system

Tasks live in `tasks/` as markdown files.

Frontmatter keys: `state`, `priority`, `size`, `dependsOn`.

**Working:** read task → check deps → clarify if needed → execute.

**Recording:** create a file in `tasks/` → brief description → confirm with user.

Grooming rules: `docs/tasks-grooming.md`.
