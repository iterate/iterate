## Findings

- P1 — [agent-durable-object.ts:94](/Users/jonastemplestein/.herdr/worktrees/iterate/stream-runner/apps/os/src/domains/agents/agent-durable-object.ts:94): the cutover does not adopt legacy shared keepalive debt. The new recovery reads only `stream-processor:agent:keepalive`, not an already-armed `stream-processor-host:keepalive`. Exact failure: legacy acknowledges `agent/llm-request-scheduled`, arms its background debounce, then the deploy evicts the DO before the timer fires. The old alarm boots the new code, but every new per-slug keepalive sees no record, appends no `agent/revived`, and the platform alarm is consumed. Because the migrated cursor is already at raw head, no frame invokes `onCaughtUp`; the lost-timer repair at [agent-processor-implementation.ts:648](/Users/jonastemplestein/.herdr/worktrees/iterate/stream-runner/apps/os/src/domains/agents/agent-processor-implementation.ts:648) never runs. A scheduled request has neither the expiry obligation at line 509 nor the requested-phase backstop at line 619, so the user turn remains stranded until an unrelated event arrives. An acknowledged, started LLM attempt can likewise remain open without its crash-cancel pulse.

- P1 — [github-agent-processor-implementation.ts:219](/Users/jonastemplestein/.herdr/worktrees/iterate/stream-runner/apps/os/src/domains/repos/github-agent-processor-implementation.ts:219): collaborator trust survives a failed frame. `active` and `verifiedMentionOffsets` are mutated before the append at line 241, but are reset only on successful `onCaughtUp` at line 123 or a route boundary at line 138. Exact failure: mention N has an inconclusive association; its check returns true, setting `active` and recording N, but the verification/message append rejects. The cursor remains before N and `onCaughtUp` does not run. On same-incarnation retry, N’s check now returns false—access was revoked or GitHub now returns 404—so no durable verification is emitted. Nevertheless, trusted non-mention N+1 passes line 205 using stale `active=true`, and its rendered state also marks N trusted using the stale offset set. Legacy `processEventBatch` cleared this transient state in `finally`; strict per-event ordering does not provide an equivalent failure boundary.

- P1 — [agent-durable-object.ts:164](/Users/jonastemplestein/.herdr/worktrees/iterate/stream-runner/apps/os/src/domains/agents/agent-durable-object.ts:164): Telegram is registered without durable recovery even though its send is consequential blocking work at [telegram-agent-processor-implementation.ts:212](/Users/jonastemplestein/.herdr/worktrees/iterate/stream-runner/apps/os/src/domains/integrations/telegram-agent-processor-implementation.ts:212). Exact failure: a `telegram/send-requested` frame reaches the Bot API, then the Agent DO and Stream DO disappear before the marker and cursor commit. There is no per-slug alarm to cold-boot either side, so a quiet thread can remain without its reply or journal marker indefinitely. If the vendor send succeeded, a later unrelated wake replays it and can duplicate the Telegram message. The comment’s “the spine redelivers” assumption is precisely the simultaneous-death/zero-lag case the legacy shared keepalive was added to cover.

- P1 — [agent-durable-object.ts:238](/Users/jonastemplestein/.herdr/worktrees/iterate/stream-runner/apps/os/src/domains/agents/agent-durable-object.ts:238): Email has the same recovery regression. Its inbound email’s only path to the LLM is the blocking transcription at [email-agent-processor-implementation.ts:84](/Users/jonastemplestein/.herdr/worktrees/iterate/stream-runner/apps/os/src/domains/email/email-agent-processor-implementation.ts:84). If both DOs disappear while resolving attachments or before `agents/message-received` commits, no durable recovery alarm reopens the unacknowledged frame; a quiet inbox message can remain untranscribed until another event happens.

- P1 — [agent-durable-object.ts:274](/Users/jonastemplestein/.herdr/worktrees/iterate/stream-runner/apps/os/src/domains/agents/agent-durable-object.ts:274): GitHub also loses the legacy whole-ingest recovery guarantee. The consequential collaborator check and agent-message append are under the blocker at [github-agent-processor-implementation.ts:202](/Users/jonastemplestein/.herdr/worktrees/iterate/stream-runner/apps/os/src/domains/repos/github-agent-processor-implementation.ts:202). If the Agent and Stream DOs disappear during the verification call at raw head, no per-slug alarm revives the unacknowledged mention, so it can strand indefinitely. GitHub has no processor-authored `runInBackground`; the regression is that legacy recovery covered blockers and the complete sink attempt too.

## Checks that passed

- The Agent `reconcile` → `onCaughtUp` body is otherwise preserved: current undriven requests start; superseded requests settle without dialing; expired requests fail; started-but-not-live requests crash-cancel and are never re-driven.
- The runner invokes this only after reaching the observed raw head and never during reduce-only refolds.
- `llm-request-scheduled@generation:${requestGeneration}` remains generation-keyed.
- Normal completion, expiry/supersede, backstop, and cancellation lanes remain keyed by request/scheduled offsets. No revival offset enters an obligation key.
- GitHub same-drive follow-ups retain strict ordering; successful route changes reset the bridge. The regression is specifically failed-frame cleanup.
- Slack and Telegram repaint memos preserve latest-wins-once-at-head across catch-up chunks. Reduce-only refolds do not paint, and no double-paint/lost-paint regression was found.
- Slack’s consequential debounce is recovery-backed. Telegram, Email, and GitHub have no processor-authored consequential `runInBackground`, but their recovery-less blocking frames remain unsafe as described above.

No files were edited.
