# Archived experiments

The single archive for the 2026-09-11 cleanup is
[`backup/v4-celld-2026-09-11`](https://github.com/iterate/iterate/tree/backup/v4-celld-2026-09-11).
It preserves:

- V4 and Celld source, tests, examples, deployment configuration, and research.
- The earlier `apps/kernel` implementation and all `spikes`.
- `packages/v3/project-core`, `project-core-fetch`, and `project-core-ws-probe`,
  including their previously uncommitted source and evidence.
- The old simplification documents, ruminations notebook, Cloudflare OS adoption
  notes, and clean-room design history.

The current clean room is [packages/v3/project-worker](../packages/v3/project-worker/README.md).
Its active API, OAuth, and control-plane documentation remains in this branch.
To resume an archived experiment, create a separate worktree from the archive branch.
