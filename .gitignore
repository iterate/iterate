.DS_Store
node_modules
.pnpm-store
*.tsbuildinfo

# TanStack
.tanstack
build
dist

# Cloudflare
.mf
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

.cursor/rules
.rules
.clinerules/

.cache
__pycache__/

# ad-hoc ignored files/paths
*ignoreme*

# Playwright test artifacts
test-results/
playwright-report/
.last-run.json

*.db
*.sqlite
*.db-wal

*.db-shm
*.db-journal
*.sqlite-wal
*.sqlite-shm
*.sqlite-journal
.output

# opencode sessions
session-ses*.md
_code-review*.md

# local lint-staged config
lint-staged.local.cjs

# local Claude overrides
.claude/settings.local.json

playwriter-screenshot-*

# PR video recordings
pr-videos/
*.log

# The host CLI's default playback file: an artifact of running it, not source.
iterate-kit-playback.wav

# Whisper models: build-tool input, fetched on demand. 141MB — over GitHub's limit.
apps/kit/firmware/tools/models/
