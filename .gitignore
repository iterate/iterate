.DS_Store
node_modules
.pnpm-store
*.tsbuildinfo

# React Router
.react-router

# TanStack
.tanstack
build
dist

# Cloudflare
.mf
.wrangler
.dev.vars*

.dev-server
!.dev.vars.example
.env*
!.env.example

.cursor/rules
.rules
.clinerules/

.cache
__pycache__/

# Experiment artifacts
experiments/*/disk-config.json

# ad-hoc ignored files/paths
*ignoreme*
evalite-export

# Playwright test artifacts
test-results/
playwright-report/
.last-run.json

*.db
*.sqlite
*.db-wal

# sqlfu scratch databases
.sqlfu/
*.db-shm
*.db-journal
*.sqlite-wal
*.sqlite-shm
*.sqlite-journal
.output

# opensrc - source code for packages
opensrc/

# opencode sessions
session-ses*.md
_code-review*.md

# local lint-staged config
lint-staged.local.cjs

# local Claude overrides
.claude/settings.local.json

playwriter-screenshot-*

# PR video recordings
pr-videos/
*.log
experiments/archil-node-modules/disk-config.json

# Local alchemy-era dev artifacts (local dev secrets + miniflare state); never commit.
.alchemy/

# Voicelab run evidence: audio both directions plus metrics, one dir per run.

# The host CLI's default playback file: an artifact of running it, not source.
iterate-kit-playback.wav

# Whisper models: build-tool input, fetched on demand. 141MB — over GitHub's limit.
apps/kit/firmware/tools/models/

# Local artifacts left by retired apps.
apps/docs/dist-package/
apps/mobile/.expo/
apps/mobile/expo-env.d.ts

# Retired app directories may contain local build/development state.
apps/*/wrangler.jsonc
apps/os/
apps/auth/
apps/semaphore/
apps/streams-example-app/
apps/dummy-petshop/
