# AI engineer workshop

This directory is now just the publishable workshop package.

- `sdk.ts` re-exports the shared events SDK from `apps/events-contract`
- `cli.ts` runs workshop scripts from the current working directory
- `pnpm w` runs the source CLI locally from this repo

Published preview packages are built from this folder directly.
