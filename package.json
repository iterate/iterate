{
  "name": "iterate-root",
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "dev": "sh -c 'export VITE_AUTH_APP_ORIGIN=http://localhost:7101; export VITE_PUBLIC_URL=http://localhost:7101; export ITERATE_OAUTH_ISSUER=http://localhost:7101/api/auth; (cd apps/auth && doppler run --preserve-env=VITE_AUTH_APP_ORIGIN,VITE_PUBLIC_URL -- pnpm dev:local) & (cd apps/os && doppler run --preserve-env=DEV_TUNNEL,ITERATE_OAUTH_ISSUER -- pnpm dev:local); wait'",
    "build": "pnpm -r --parallel --filter '!iterate' build",
    "typecheck": "pnpm -r --parallel --filter '!iterate' typecheck",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "os": "cd apps/os && pnpm",
    "events": "cd apps/events && pnpm",
    "skills:sync": "npx skills add . --skill '*' -a claude-code -a opencode -g -y",
    "skills:list": "npx skills list -g -a claude-code -a opencode",
    "preview": "node ./packages/iterate/bin/iterate.js --local-router ./scripts/preview/router.ts local-router preview",
    "lint": "oxlint . --threads 1 --deny-warnings",
    "lint:check": "pnpm lint",
    "lint:fix": "oxlint . --fix",
    "knip": "knip --workspace apps/example --workspace apps/example-contract --workspace apps/events --workspace apps/events-contract --workspace apps/os --workspace apps/os-contract --workspace apps/semaphore --workspace apps/semaphore-contract --workspace packages/shared",
    "prepare": "is-ci || husky",
    "vitest": "pnpm --dir apps/os test",
    "test": "pnpm -r --parallel --filter '!iterate' test",
    "test:onboarding": "pnpm -r --parallel --filter '!iterate' test:onboarding",
    "test:onboarding:production": "pnpm -r --parallel --filter '!iterate' test:onboarding:production",
    "seed:tunnel-pool": "pnpm --dir apps/semaphore run seed:tunnel-pool",
    "db:migrate": "pnpm --dir apps/os sqlfu:migrate",
    "db:generate": "pnpm --dir apps/os sqlfu:generate",
    "db:studio": "pnpm --dir apps/os sqlfu:ui",
    "clean": "rm -rf ./**/node_modules",
    "workflows": "cd .github/ts-workflows && pnpm generate",
    "super-reset": "pnpm i && pnpm -r --parallel --filter '!iterate' super-reset"
  },
  "devDependencies": {
    "@tanstack/eslint-plugin-router": "^1.139.0",
    "@types/node": "^24.10.1",
    "@typescript/native-preview": "^7.0.0-dev.20260203.1",
    "dedent": "^1.7.0",
    "eslint-plugin-codegen": "0.34.1",
    "eslint-plugin-eslint-comments": "^3.2.0",
    "eslint-plugin-import": "^2.32.0",
    "eslint-plugin-unicorn": "^64.0.0",
    "esquery": "1.6.0",
    "husky": "9.1.7",
    "is-ci": "4.1.0",
    "knip": "^6.0.5",
    "lint-staged": "^16.2.7",
    "oxfmt": "^0.35.0",
    "oxlint": "^1.50.0",
    "skills": "^1.4.5",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  },
  "packageManager": "pnpm@10.24.0",
  "dependencies": {
    "@octokit/rest": "^22.0.0",
    "@orpc/client": "1.13.14",
    "@orpc/server": "1.13.14",
    "zod": "^4.3.6"
  }
}
