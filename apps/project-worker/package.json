{
  "name": "project-worker",
  "private": true,
  "type": "module",
  "description": "Project worker — the runner. Loads + serves a project's confined config worker (the project config worker) via Worker Loader. Dialed by the control plane over a service binding (same account) or HTTP + shared secret (cross-account). Pure-play. See docs/simplification/wayfinder/deployment-topologies.md.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260621.1",
    "typescript": "^5.9.3",
    "wrangler": "4.107.0"
  }
}
