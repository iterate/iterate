{
  "name": "project-worker",
  "private": true,
  "type": "module",
  "description": "The clean-room inner core — ONE npm package (D34, no packages/itx). src/ holds the shared core (itx-durable-object, stream-durable-object, core/*) + both worker entrypoints: worker.ts (the project worker / capability host, deployed via wrangler.jsonc) and control-plane-shell.ts (the outer shell, deployed via wrangler.control-plane.jsonc). See apps/project-worker/BUILD-LOG.md.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:control-plane": "wrangler deploy --config wrangler.control-plane.jsonc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "capnweb": "npm:@iterate-com/capnweb@^0.10.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260621.1",
    "typescript": "^5.9.3",
    "wrangler": "4.107.0"
  }
}
