# scripts/build.ts: the two generated modules (their .d.ts siblings are committed). Only the
# declarations belong in git — a stale generated .ts here shadows them for tsc.
/src/generated/*
!/src/generated/*.d.ts
# scripts/build.ts: the capnweb fork's browser bundle, copied beside the consent page
/public/capnweb.js

/wrangler.jsonc
# scripts/build.ts: the self-host config (SELF-HOSTING.md), the same bindings with no ids
/wrangler.self-host.jsonc
