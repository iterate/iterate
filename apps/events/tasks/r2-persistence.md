when DOs get too large, we should persist them in chunks of a given size into R2

this is also where i would apply filtering by event type to e.g. prune streaming chunks or raw voice PCM out of the object

as always, persistence config would be defined by events
