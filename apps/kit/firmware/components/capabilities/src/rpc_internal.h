#ifndef ITERATE_KIT_CAPABILITIES_RPC_INTERNAL_H
#define ITERATE_KIT_CAPABILITIES_RPC_INTERNAL_H

/*
 * The argument helpers are public now — see the header below for why. This
 * file remains so the modules in this component keep one short include.
 */
#include "iterate/kit/capabilities/arguments.h"

#endif
