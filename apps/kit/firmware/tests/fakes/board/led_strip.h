#ifndef ITERATE_KIT_TEST_LED_STRIP_H
#define ITERATE_KIT_TEST_LED_STRIP_H
/** Pixel order is only data in the pure board tests. */
typedef enum { LED_PIXEL_FORMAT_GRB, LED_PIXEL_FORMAT_RGB } led_pixel_format_t;
#endif
