// Generated by `sqlfu generate`. Do not edit.

export * from "./hostnames.sql.js";
export * from "./integration-routes.sql.js";
export * from "./invitations.sql.js";
export * from "./oauth-grants.sql.js";
export * from "./organizations.sql.js";
export * from "./projects.sql.js";
export * from "./users.sql.js";

export const sqlfuQuerySources = [
	{ sqlFile: "hostnames.sql", generatedFile: "hostnames.sql.ts", sourceSql: "/** @name projectsByHostnames */\nselect h.hostname, p.id, p.slug, p.org_id as orgId\nfrom project_hostnames h\njoin projects p on p.id = h.project_id\nwhere h.hostname in (:hostnames);\n\n/** @name claimHostname */\ninsert into project_hostnames (hostname, project_id)\nselect :hostname, p.id\nfrom projects p\nwhere p.id = :projectId\n  and not exists (\n    select 1 from project_hostnames h\n    where h.hostname in (:selfAndAbove) and h.project_id <> p.id\n  )\non conflict (hostname) do nothing;\n\n/** @name releaseHostname */\ndelete from project_hostnames where hostname = :hostname and project_id = :projectId;\n\n/** @name setPrimaryHostname */\ninsert into project_primary_hostnames (project_id, hostname) values (:projectId, :hostname)\non conflict (project_id) do update set hostname = excluded.hostname;\n\n/** @name clearPrimaryHostname */\ndelete from project_primary_hostnames where project_id = :projectId;\n\n/** @name primaryHostnameOf */\nselect h.hostname\nfrom project_primary_hostnames p\njoin project_hostnames h on h.hostname = p.hostname and h.project_id = p.project_id\nwhere p.project_id = :projectId\nlimit 1;\n" },
	{ sqlFile: "integration-routes.sql", generatedFile: "integration-routes.sql.ts", sourceSql: "/** @name integrationRoute */\nselect project_id as projectId, path\nfrom integration_routes\nwhere provider = :provider and external_id = :externalId;\n\n/** @name routeIntegration */\ninsert into integration_routes (provider, external_id, project_id, path)\nselect :provider, :externalId, p.id, :path\nfrom projects p\nwhere p.id = :projectId\non conflict (provider, external_id) do nothing;\n\n/** @name releaseOtherIntegrationRoutes */\ndelete from integration_routes\nwhere exists (\n  select 1\n  from integration_routes held\n  where held.provider = :provider\n    and held.external_id = :externalId\n    and held.project_id = :projectId\n    and held.path = :path\n    and held.project_id = integration_routes.project_id\n    and held.path = integration_routes.path\n    and (\n      held.provider <> integration_routes.provider\n      or held.external_id <> integration_routes.external_id\n    )\n);\n\n/** @name releaseIntegrationRoutes */\ndelete from integration_routes where project_id = :projectId and path = :path;\n\n/** @name releaseRoutesOfDeletedProject */\ndelete from integration_routes\nwhere project_id = :projectId and project_id not in (select id from projects);\n\n/** @name moveIntegrationRoute */\nupdate integration_routes\nset project_id = :toProjectId, path = :toPath\nwhere provider = :provider\n  and external_id = :externalId\n  and project_id = :fromProjectId\n  and path = :fromPath;\n\n/** @name releaseIntegrationRoute */\ndelete from integration_routes\nwhere provider = :provider\n  and external_id = :externalId\n  and project_id = :projectId\n  and path = :path;\n" },
	{ sqlFile: "invitations.sql", generatedFile: "invitations.sql.ts", sourceSql: "/** @name insertInvitation */\ninsert into invitations (id, token_hash, org_id, role, email_hint, created_by, created_at, expires_at)\nselect :id, :tokenHash, o.id, :role, :emailHint, :createdBy, :createdAt, :expiresAt\nfrom organizations o\nwhere o.id = :orgId\n  and (:asOperator = 1 or exists (\n    select 1 from memberships a\n    where a.org_id = o.id and a.user_id = :actorId and a.role = 'owner'\n  ));\n\n/** @name invitationById */\nselect\n  id,\n  org_id as orgId,\n  role,\n  email_hint as emailHint,\n  expires_at as expiresAt,\n  revoked_at as revokedAt,\n  accepted_by as acceptedBy\nfrom invitations\nwhere id = :id\nlimit 1;\n\n/** @name revokeInvitation */\nupdate invitations set revoked_at = :revokedAt\nwhere id = :id\n  and org_id = :orgId\n  and accepted_by is null\n  and revoked_at is null\n  and (:asOperator = 1 or exists (\n    select 1 from memberships a\n    where a.org_id = invitations.org_id and a.user_id = :actorId and a.role = 'owner'\n  ));\n\n/** @name invitationByToken */\nselect\n  i.id,\n  i.org_id as orgId,\n  i.role,\n  i.email_hint as emailHint,\n  i.expires_at as expiresAt,\n  i.revoked_at as revokedAt,\n  i.accepted_by as acceptedBy,\n  o.name as orgName,\n  (select m.role from memberships m where m.org_id = i.org_id and m.user_id = :userId) as memberRole\nfrom invitations i\njoin organizations o on o.id = i.org_id\nwhere i.token_hash = :tokenHash\nlimit 1;\n\n/** @name acceptInvitation */\nupdate invitations set accepted_by = :acceptedBy, accepted_at = :acceptedAt, acceptance_id = :acceptanceId\nwhere token_hash = :tokenHash\n  and accepted_by is null\n  and revoked_at is null\n  and expires_at > :now\n  and not exists (\n    select 1 from memberships m where m.org_id = invitations.org_id and m.user_id = :userId\n  );\n\n/** @name insertAcceptedMembership */\ninsert into memberships (org_id, user_id, role)\nselect org_id, accepted_by, role\nfrom invitations\nwhere token_hash = :tokenHash and acceptance_id = :acceptanceId;\n" },
	{ sqlFile: "oauth-grants.sql", generatedFile: "oauth-grants.sql.ts", sourceSql: "/** @name oauthGrant */\nselect value from oauth_grants where key = :key and (expires_at is null or expires_at > :now);\n\n/** @name listOAuthGrants */\nselect key, expires_at as expiresAt\nfrom oauth_grants\nwhere key > max(:cursor, :prefix)\n  and key < :end\n  and (expires_at is null or expires_at > :now)\norder by key\nlimit :limit;\n\n/** @name purgeExpiredOAuthGrants */\ndelete from oauth_grants where expires_at <= :now;\n\n/** @name upsertOAuthGrant */\ninsert into oauth_grants (key, value, expires_at) values (:key, :value, :expiresAt)\non conflict (key) do update set value = excluded.value, expires_at = excluded.expires_at;\n\n/** @name deleteOAuthGrant */\ndelete from oauth_grants where key = :key;\n" },
	{ sqlFile: "organizations.sql", generatedFile: "organizations.sql.ts", sourceSql: "/** @name organizationById */\nselect o.id, o.name, coalesce((select count(*) from projects p where p.org_id = o.id), 0) as projects\nfrom organizations o\nwhere o.id = :id;\n\n/** @name listOrganizations */\nselect o.id, o.name, coalesce((select count(*) from projects p where p.org_id = o.id), 0) as projects\nfrom organizations o\norder by o.name, o.id;\n\n/** @name organizationRole */\nselect\n  o.id,\n  (select m.role from memberships m where m.org_id = o.id and m.user_id = :userId) as role,\n  coalesce((select count(*) from projects p where p.org_id = o.id), 0) as projects\nfrom organizations o\nwhere o.id = :orgId;\n\n/** @name listMembers */\nselect m.user_id as userId, u.email, m.role\nfrom memberships m\njoin users u on u.id = m.user_id\nwhere m.org_id = :orgId\norder by u.email;\n\n/** @name memberOf */\nselect u.id, (select m.role from memberships m where m.org_id = :orgId and m.user_id = u.id) as role\nfrom users u\nwhere u.id = :userId or u.email = :email\nlimit 1;\n\n/** @name accessibleOrganizations */\nselect o.id, o.name, m.role, coalesce((select count(*) from projects p where p.org_id = o.id), 0) as projects\nfrom memberships m\njoin organizations o on o.id = m.org_id\nwhere m.user_id = :userId\norder by o.name, o.id;\n\n/** @name accessibleProjects */\nselect p.id, p.slug, p.org_id as orgId, m.role\nfrom projects p\njoin memberships m on m.org_id = p.org_id\nwhere m.user_id = :userId\norder by p.slug;\n\n/** @name insertOrganization */\ninsert into organizations (id, name) values (:id, :name);\n\n/** @name insertOwner */\ninsert into memberships (org_id, user_id, role)\nselect o.id, :userId, 'owner' from organizations o where o.id = :orgId;\n\n/** @name renameOrganization */\nupdate organizations set name = :name\nwhere id = :id\n  and (:asOperator = 1 or exists (\n    select 1 from memberships a\n    where a.org_id = organizations.id and a.user_id = :actorId and a.role = 'owner'\n  ));\n\n/** @name deleteOrganization */\ndelete from organizations\nwhere id = :id\n  and not exists (select 1 from projects p where p.org_id = organizations.id)\n  and (:asOperator = 1 or exists (\n    select 1 from memberships a\n    where a.org_id = organizations.id and a.user_id = :actorId and a.role = 'owner'\n  ));\n\n/** @name upsertMembership */\ninsert into memberships (org_id, user_id, role)\nselect o.id, u.id, :role\nfrom organizations o, users u\nwhere o.id = :orgId\n  and (u.id = :userId or u.email = :email)\n  and (:asOperator = 1 or exists (\n    select 1 from memberships a\n    where a.org_id = o.id and a.user_id = :actorId and a.role = 'owner'\n  ))\non conflict (org_id, user_id) do update set role = excluded.role\nwhere memberships.role <> 'owner'\n  or excluded.role = 'owner'\n  or (select count(*) from memberships c where c.org_id = memberships.org_id and c.role = 'owner') > 1;\n\n/** @name deleteMembership */\ndelete from memberships\nwhere org_id = :orgId\n  and exists (\n    select 1 from users u\n    where u.id = memberships.user_id and (u.id = :userId or u.email = :email)\n  )\n  and (\n    role <> 'owner'\n    or (select count(*) from memberships c where c.org_id = memberships.org_id and c.role = 'owner') > 1\n  )\n  and (:asOperator = 1 or exists (\n    select 1 from memberships a\n    where a.org_id = memberships.org_id and a.user_id = :actorId and a.role = 'owner'\n  ));\n" },
	{ sqlFile: "projects.sql", generatedFile: "projects.sql.ts", sourceSql: "/** @name projectsByRef */\nselect id, slug, org_id as orgId from projects where id = :id or slug = :slug;\n\n/** @name listProjects */\nselect id, slug, org_id as orgId from projects order by slug;\n\n/** @name insertProject */\ninsert into projects (id, slug, org_id)\nselect :id, :slug, o.id from organizations o where o.id = :orgId\non conflict do nothing;\n\n/** @name insertMemberProject */\ninsert into projects (id, slug, org_id)\nselect :id, :slug, m.org_id from memberships m where m.org_id = :orgId and m.user_id = :userId\non conflict do nothing;\n\n/** @name insertAdminOrganization */\ninsert into organizations (id, name)\nselect :orgId, 'admin'\nwhere not exists (select 1 from projects p where p.slug = :slug or p.id = :projectId)\non conflict (id) do nothing;\n\n/** @name insertPersonalOrganization */\ninsert into organizations (id, name)\nselect :id, :name\nfrom users u\nwhere u.id = :userId\n  and not exists (select 1 from memberships m where m.user_id = u.id)\n  and not exists (select 1 from projects p where p.slug = :slug);\n\n/** @name insertFirstOrganizationProject */\ninsert into projects (id, slug, org_id)\nselect :id, :slug, m.org_id\nfrom memberships m\njoin organizations o on o.id = m.org_id\nwhere m.user_id = :userId\norder by o.name, o.id\nlimit 1\non conflict do nothing;\n\n/** @name firstOrganizationOf */\nselect o.id\nfrom memberships m\njoin organizations o on o.id = m.org_id\nwhere m.user_id = :userId\norder by o.name, o.id\nlimit 1;\n\n/** @name deleteProject */\ndelete from projects\nwhere id = :id\n  and (:asOperator = 1 or exists (\n    select 1 from memberships a\n    where a.org_id = projects.org_id and a.user_id = :actorId and a.role = 'owner'\n  ));\n" },
	{ sqlFile: "users.sql", generatedFile: "users.sql.ts", sourceSql: "/** @name userByRef */\nselect id, email from users where id = :id or email = :email limit 1;\n\n/** @name listUsers */\nselect id, email from users order by email;\n\n/** @name insertUserIfNew */\ninsert into users (id, email) values (:id, :email) on conflict (email) do nothing;\n\n/** @name updateUserEmail */\nupdate users set email = :email where id = :id;\n\n/** @name identityUser */\nselect u.id, u.email, (select count(*) from identities j where j.user_id = u.id) as sign_ins\nfrom identities i\njoin users u on u.id = i.user_id\nwhere i.provider = :provider and i.subject = :subject\nlimit 1;\n\n/** @name insertUserUnlessLinked */\ninsert into users (id, email)\nselect :id, :email\nwhere not exists (select 1 from identities i where i.provider = :provider and i.subject = :subject)\non conflict (email) do nothing;\n\n/** @name insertIdentity */\ninsert into identities (provider, subject, user_id)\nselect :provider, :subject, u.id from users u where u.email = :email\non conflict do nothing;\n" },
];
