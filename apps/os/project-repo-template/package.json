{
  "name": "iterate-project-worker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Iterate project worker. Dependencies listed here are installed by the worker build pipeline when the worker is bundled. `iterate` is a runtime dependency: worker.ts imports the stream-processor machinery from iterate/sdk (the capability types ride along).",
  "dependencies": {
    "@slack/web-api": "^7.14.1",
    "iterate": "https://pkg.pr.new/iterate/iterate/iterate@main"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "typescript": "^5.9.3"
  }
}
