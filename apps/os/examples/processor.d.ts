// Types for the SDK module apps/os injects as "./processor.js" into every loaded isolate that
// imports it (apps/os/src/context/worker-loader.ts): the `iterate/next/sdk` bundle.
export * from "iterate/next/sdk";
