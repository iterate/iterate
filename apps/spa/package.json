{
  "name": "@iterate-com/spa",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 8799 --bind 127.0.0.1 --directory public",
    "deploy": "tsx scripts/deploy.ts",
    "build": "tsx scripts/build.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "fflate": "catalog:",
    "trpc-cli": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "wrangler": "catalog:",
    "zod": "catalog:"
  }
}
