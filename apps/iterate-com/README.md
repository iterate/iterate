# iterate.com website

## Deploy

Prereqs:

- `pnpm install` at repo root
- `doppler setup --project iterate-com --config prd` (or `dev` / `dev_<name>`)

Deploy:

- `pnpm -C apps/iterate-com run deploy`

## Local dev

- `pnpm -C apps/iterate-com run dev:website`
