# iterate.com website

## Deploy

Prereqs:

- `pnpm install` at repo root
- `doppler setup --project os-legacy-backup --config prd` (or `preview_N`/`dev`)

Deploy:

- `pnpm -C apps/iterate-com run deploy`

## Local dev

- `pnpm -C apps/iterate-com run dev:website`
