/** The scopes the admin app asks for at sign-in: `admin` is what it is for — every project and
 *  person, for a person the platform's `admins` lists (apps/os consent.ts offers it to no one else). */
export const adminScopes = ["iterate", "admin"];
