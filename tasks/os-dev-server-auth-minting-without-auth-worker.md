---
state: done
priority: high
size: small
tags: [auth, os, dev-server, agents]
---

> _This was generated by AI during triage._

> **Update (2026-07-07, branch `os-ios-app`):** hit this gap building the
> mobile app's live e2e, which needs forge bearer tokens against a local dev
> server, and first fixed it locally in `apps/os/scripts/dev.ts`
> (fetch-and-merge into the spawned vite child's env). Main independently
> landed the fix below (PR #1706) in the same window; on merging main into
> `os-ios-app`, the branch-local `dev.ts` version was dropped as redundant
> and the e2e now depends on main's fix instead. Verified live either way:
> forged bearer tokens authenticate over `/api` against a fresh dev server
> (`apps/mobile/e2e/chat-roundtrip.e2e.test.ts` passes).

# Local OS dev auth minting lacks a reliable no-auth-worker path

## Resolution

Resolved in PR #1706. Local OS dev now derives
`APP_CONFIG_ITERATE_AUTH__JWKS` from `AUTH_FORGE_PRIVATE_JWK` while generating
Wrangler config, so `pnpm auth:mint --browser-url` does not depend on a
Doppler JWKS snapshot or a separately deployed auth worker. Deploys still bake
`APP_CONFIG_ITERATE_AUTH__JWKS`, but they fetch live auth JWKS directly and
merge the forge public key at deploy time.

## Problem

The documented local agent/debug path says a developer can start OS locally and
mint a browser session:

```bash
doppler run --project os --config dev -- pnpm auth:mint --browser-url
```

In practice, against a fresh local `apps/os` dev server, navigating to the
printed `/api/iterate-auth/session-from-token?...` URL can fail with:

```text
Token validation failed: JWKSNoMatchingKey: no applicable key found in the JSON Web Key Set
```

That means the local dev server is not reliably trusting the same forge public
key that `scripts/auth/mint-session.ts` uses to sign the session tokens.

After injecting the forge JWKS manually, the next failure was:

```text
JWTClaimValidationFailed: unexpected "aud" claim value
```

`auth:mint` signs local loopback access tokens for `http://localhost`, while
the OS verifier was deriving the resource from the live request origin
(`http://localhost:<port>`) when no `APP_CONFIG_BASE_URL` was configured.

After restarting the dev server with a synthesized forge JWKS and minting a
session with the required project claims, the one-shot browser URL did work and
loaded `/projects/screenshot/sandboxes` without the auth worker. The bug is
that this working path is not automated or documented; a clean local server
starts without the JWKS trust material needed by `auth:mint`.

## Why this matters

Headless agents need a deterministic way to authenticate into the project UI
without Google OAuth, email OTP, or a separate local/auth preview worker. This
is especially important for screenshot/debug flows where the agent needs to
open routes under `/_app/projects/...` from a clean browser profile.

The deploy path already handles this by baking a static auth JWKS with the
forge public key via `scripts/lib/bake-auth-jwks.ts`. Local dev should have an
equally documented and automated equivalent.

## Repro

From a clean browser profile:

```bash
doppler run --project os --config dev -- pnpm --dir apps/os dev restart --detach
URL="$(doppler run --project os --config dev -- pnpm --silent auth:mint --admin --browser-url --return-to /projects)"
open "$URL"
```

Observed response:

```text
Token validation failed: JWKSNoMatchingKey: no applicable key found in the JSON Web Key Set
```

Manual workaround that confirmed the likely cause:

1. Derive the public JWK from `AUTH_FORGE_PRIVATE_JWK`.
2. Inject it into `APP_CONFIG_ITERATE_AUTH__JWKS`.
3. Restart `pnpm dev` with `doppler run --preserve-env=APP_CONFIG_ITERATE_AUTH__JWKS`.

After that, `session-from-token` progressed past key selection and exposed the
loopback audience mismatch above.

Additional reproduction notes from the sandbox UI debugging session:

- `APP_CONFIG_ITERATE_AUTH__JWKS` was not listed in the local
  `generate-wrangler-config.ts` optional secrets, so Wrangler/Vite did not bind
  it locally even when the process environment contained it.
- Once the JWKS binding was available, the verifier needed to normalize
  localhost resources the same way as `auth:mint`, without the ephemeral dev
  server port.
- A protected project route also needs project claims in the minted session;
  an admin-only forged session can authenticate but still falls through to the
  project-access flow.

## Expected behavior

One of these should be true and documented:

- `pnpm dev` automatically bakes/injects a local static JWKS containing the
  forge public key when `AUTH_FORGE_PRIVATE_JWK` is present, or
- `pnpm auth:mint --browser-url` targets a local-only session path that does
  not require the auth worker's runtime JWKS at all, or
- there is a first-class command such as `pnpm dev login-url` that starts OS,
  prepares the local JWKS/session machinery, and prints a browser URL.

The happy path should work from a clean browser profile with no auth-worker
availability requirement.

## Suggested fix

Reuse the deploy-time logic from `scripts/lib/bake-auth-jwks.ts` in the local
dev startup path:

- `apps/os/scripts/dev.ts` already has `doppler.loadOsSecrets(...)`.
- Keep `APP_CONFIG_ITERATE_AUTH__JWKS` exposed to local Wrangler/Vite bindings.
- When starting without `--skip-doppler`, load the configured OS secrets.
- If `APP_CONFIG_ITERATE_AUTH__JWKS` is absent but `AUTH_FORGE_PRIVATE_JWK` is
  present, synthesize a JWKS by stripping the private `d` field and appending
  the forge public key.
- Pass the resulting `APP_CONFIG_ITERATE_AUTH__JWKS` into the spawned Vite
  process.
- Normalize loopback auth resources to `http://localhost` so OS and
  `auth:mint` agree on the expected audience while keeping non-loopback hosts
  exact.
- Add an integration-style test or scripted smoke that starts the dev server,
  runs `pnpm auth:mint --browser-url`, and asserts
  `/api/iterate-auth/session-from-token` redirects instead of returning a JWKS
  validation error.

## Acceptance criteria

- `doppler run --project os --config dev -- pnpm --dir apps/os dev restart --detach`
  followed by `pnpm auth:mint --browser-url` works without a separate auth
  worker deploy.
- A clean browser can open a protected route such as `/projects/<slug>/sandboxes`
  after using the minted browser URL.
- The dev docs explicitly describe the auth-mint path for agents and clean
  browser profiles.
- The failure mode is covered by a smoke test or documented manual test.
