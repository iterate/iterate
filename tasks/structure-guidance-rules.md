---
status: in-progress
size: small
---

# Add structure guidance rules

## Status summary

Specified and ready to apply. The branch will contain only this task record and the user's exact
existing changes under `rules/structure/`; no rule wording, filenames, or formatting will be edited.

## Spec

- [ ] Preserve the three new `rules/structure` files byte-for-byte. *Pending transfer from the root
      worktree.*
- [ ] Remove the superseded `no-small-single-use-helper.md` rule. *Pending transfer from the root
      worktree.*
- [ ] Verify the resulting `rules/structure` diff matches the root worktree exactly. *Pending
      byte/diff comparison.*
- [ ] Open a draft PR based on current `origin/main`. *Pending.*

## Decisions and assumptions

- “Verbatim” means misspellings, punctuation, examples, and formatting are intentional.
- Unrelated root-worktree changes are out of scope and will remain untouched.
