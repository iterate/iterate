# JonasLand Architecture

# Statements that need sorting

- iterate provides two major things that can be used together or independently
  - iterate deployments - these are like general purpose AI homelab deployments
  - the iterate hosted service (via the "os worker") with pass through billing, secret storage etc

- Concerns that the os app owns
  - organisations, projects and billing
  - managing deployments for the projects
  - iterate owned credentials and pass-through billing
  - iterate-owned oauth client connections
  - webhook routing
  - users and authentication
  - egress proxy with secret substitution and human in the loop approval
  - ingress proxy with custom domain support

### Service architecture

- Within each deployment we operate an HTTP-based service oriented architecture
- Architecture at a glance:
  - iptables rules on machines/containers force all ingress and egress traffic through Caddy
  - Caddy reads rule files generated by our "registry" service, which effectively maps hostnames to ip+port combos
  - We only support HTTP services because we want to keep the door open to deploying to cloudflare and other edge networks in the future
- This architecture allows different agents to make small changes to services and keep the blast radius and merge conflicts minimal

# Future direction

- More concerns will move into the deployments and out of the os worker. Most importantly secrets storage, substitution and the human in the loop egress control. This is so we can 1) more easily end to end test everything and 2) so homebrew hackers are more powerful

#

## Goal

Smallest working single-container homelab stack:

- Pidnap is the main process manager inside one Docker container.
- Caddy runs as a pidnap-managed process.
- Registry service runs as a pidnap-managed process (in-memory route table + ORPC API).
- All ingress enters via a single host-mapped URL to Caddy on container port `80`.
- Outbound TCP `:80/:443` is transparently redirected to Caddy with iptables.

No custom Caddy plugins. No Docker socket mount. No sibling containers.

## Runtime flow

1. Container starts `entry.sh`.
2. `entry.sh` installs `nat OUTPUT` rules redirecting `:80/:443` to local Caddy.
3. `entry.sh` starts pidnap (`pidnap.config.ts`).
4. Pidnap starts `caddy` and `registry`.

## Service routing

- Caddy listens on port `80`.
- `Host: pidnap.iterate.localhost` routes to `127.0.0.1:9876`.
- `Host: registry.iterate.localhost` routes to `127.0.0.1:8777`.
- `Host: caddy.iterate.localhost` routes to Caddy admin `127.0.0.1:2019`.
- Any outbound HTTP(S) from root-owned processes is redirected to Caddy by iptables.
- Caddy admin API listens on `0.0.0.0:2019` inside the container.
- Admin API is reached externally via the single ingress URL + `Host: caddy.iterate.localhost`.
- Registry ORPC listens on `0.0.0.0:8777` inside the container.

## E2E scope

`jonasland/e2e` validates:

- Pidnap RPC is up.
- Caddy is running and typed SDK client access works.
- Registry service is running and typed client access works.
- Caddy rejects browser-style navigation access to admin endpoints.
- Caddy can proxy to pidnap via host routing.
- iptables redirect rules are present and outbound HTTP is intercepted by Caddy.
- Pidnap can imperatively add/start/stop/remove runtime processes after baseline health.

## Commands

- Build image: `pnpm --filter ./jonasland/sandbox build`
- Run e2e: `pnpm --filter ./jonasland/e2e test:e2e`
