import { test } from "@playwright/test";

test.describe("egress", () => {
  test("browser-driven actions can trigger egress requests through the expected path", async () => {
    test.fixme();
  });
});
