import { test } from "@playwright/test";

test.describe("example service ui", () => {
  test("the example service exposes a UI that can issue an egress request", async () => {
    test.fixme();
  });
});
