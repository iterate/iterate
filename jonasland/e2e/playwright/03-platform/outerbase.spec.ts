import { test } from "@playwright/test";

test.describe("outerbase service", () => {
  test("the outerbase service is usable through browser-driven end-to-end access", async () => {
    test.fixme();
  });
});
