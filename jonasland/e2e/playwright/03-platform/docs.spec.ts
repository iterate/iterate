import { test } from "@playwright/test";

test.describe("docs service", () => {
  test("the docs service is usable through browser-driven end-to-end access", async () => {
    test.fixme();
  });
});
