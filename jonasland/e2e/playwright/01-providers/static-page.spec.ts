import { test } from "@playwright/test";

test.describe("static page", () => {
  test("a browser can load a static page served from a minimal alpine-based container", async () => {
    test.fixme();
  });
});
