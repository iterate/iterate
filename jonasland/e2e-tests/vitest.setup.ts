import { afterAll, afterEach, beforeAll } from "vitest";

beforeAll(() => {
  // Reserved for MSW wiring in smoke tests.
});

afterEach(() => {
  // Reserved for test-level runtime cleanup.
});

afterAll(() => {
  // Reserved for global cleanup.
});
