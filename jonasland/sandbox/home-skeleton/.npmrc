prefix=/home/iterate/.npm-global
store-dir=/home/iterate/.pnpm-store
# Avoid hoisting so CLI tools (drizzle-kit) resolve local deps in apps/daemon.
node-linker=isolated
hoist-pattern[]=
public-hoist-pattern[]=
